<h1>This is the welcome page</h1>

<% if current_user %>
  <p> <%= "Hello #{current_user.name}"%> </p>
  <%=link_to "Logout", logout_path %>
  <%= render "layous/user_home" %>


<% else %>
  <%=link_to "Login", login_path %>
  <%=link_to "Sign Up", signup_path %>
<% end %>
